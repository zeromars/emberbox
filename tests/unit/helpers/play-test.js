import { play } from 'emberbox/helpers/play';
import { module, test } from 'qunit';

module('Unit | Helper | play');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = play([42]);
  assert.ok(result);
});
